## Run

Install packages
`npm install`

Run dev server 
`npm run api`

Open API documentation, then You can use all endpoints (for admin endpoints create admin and authorize)
`http://localhost:3000/reference`

